OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o

SERVER_ADDR=:8787
DATABASE_PATH=./family.db

# 非空时启用页面访问认证；登录页输入同一个值
AUTH_KEY=

# 摘要推送 email 通道（可选）；SMTP_HOST 为空时 email 发送禁用，webhook 通道不受影响
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
