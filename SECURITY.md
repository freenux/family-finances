# Security Policy

## 数据边界

Family Finances 会处理账单流水、分类、备注和汇总报表。这些数据默认存储在本地 SQLite 文件中，不应提交到 Git 仓库或公开问题区。

启用 LLM 分类时，应用会把待分类流水的 `id`、交易对方、说明和收支方向发送给配置的 OpenAI 兼容接口；不会发送金额和时间。留空 `OPENAI_API_KEY` 可完全关闭 LLM 功能。

## 部署建议

- 公开网络部署时必须设置非空 `AUTH_KEY`。
- 不要对公网暴露 SQLite 文件、备份目录或应用内部监听端口。
- 定期备份数据库，并把备份文件放在仓库外。
- 上传或分享问题复现材料前，先脱敏交易对方、说明、备注、金额和账户标识。

## 漏洞报告

请不要在公开 issue 中披露可利用漏洞或真实财务数据。优先通过仓库维护者在 GitHub 个人资料中提供的安全联系方式报告；如果没有可用联系方式，请先提交一个不含细节的 issue 说明需要私下沟通。
